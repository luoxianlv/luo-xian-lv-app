# 排查范围

- auth：用户要求分析其提供的 APK 崩溃，并在当前项目源码中修复。
- in_scope：落弦律 1.0.6 APK 的 `d5.c` 崩溃、`Analytics.record()`、当前项目的兼容性验证。
- network_profile：本机 DexClub MCP `http://127.0.0.1:8787/mcp`；只读查询官方 Android 文档及本仓库发布记录。
- 不包含发布、推送、安装或修改用户手机数据。
